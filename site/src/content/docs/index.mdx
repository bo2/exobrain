---
title: Exobrain
description: A version-controlled knowledge base your AI agent loads as context, so it works with knowledge of your world instead of starting cold every session.
template: splash
hero:
  tagline: Your agent already writes good code. It just doesn't know anything about you — your projects, your machines, your conventions, the decision you settled last month. An exobrain is where that lives.
  actions:
    - text: Create yours
      link: /start/
      icon: rocket
    - text: Why you'd want one
      link: /why/
      icon: right-arrow
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';
import Compare from '../../components/Compare.astro';

## Six ordinary moments, with and without

Same model, same prompt. The only difference is whether it has somewhere durable to read from.

<Compare scenario="You ask it to start a new piece of work">
  <Fragment slot="without">
    It branches off `main`. Your default is `trunk`.

    You correct it. It apologises and gets it right for the rest of the session.

    *Tomorrow: `main` again.*
  </Fragment>
  <Fragment slot="with">
    It branches off `trunk`, because your convention is a line in a file it read at startup.

    *You stopped noticing this months ago.*
  </Fragment>
</Compare>

<Compare scenario="A procedure you run every week">
  <Fragment slot="without">
    You paste the same four steps into the chat, in the same order, for the fourth time this month.

    *The steps live in your head and nowhere else.*
  </Fragment>
  <Fragment slot="with">
    You say "ship it." The procedure is written down as a skill; it reads the steps and follows them.

    *When the procedure changes, you edit the file once.*
  </Fragment>
</Compare>

<Compare scenario="A decision you made a month ago">
  <Fragment slot="without">
    It proposes the design you evaluated and rejected in August.

    It was a reasonable suggestion — it has no way to know you've been here. Neither of you remembers the reason you said no.

    *You rebuild the argument from scratch.*
  </Fragment>
  <Fragment slot="with">
    It reads the decision, and the reason behind it, and proposes something that accounts for both.

    *The afternoon you spent deciding stays spent.*
  </Fragment>
</Compare>

<Compare scenario="You open your other laptop">
  <Fragment slot="without">
    Every path it learned is wrong. You spend the first twenty minutes re-teaching a setup you already taught.

    *Two machines means teaching everything twice.*
  </Fragment>
  <Fragment slot="with">
    That machine's scope loads, with that machine's paths. Everything you know stays shared.

    *A third machine is one small file.*
  </Fragment>
</Compare>

<Compare scenario="You want to try a different agent">
  <Fragment slot="without">
    Start over. Whatever it had learned lived in one vendor's memory, in a format you can't read or move.

    *Switching costs you everything you'd built up.*
  </Fragment>
  <Fragment slot="with">
    Run the connector. The new agent reads the same files, and per-agent quirks sit in a sidecar.

    *Running two agents at once is a supported case, not a workaround.*
  </Fragment>
</Compare>

<Compare scenario="It needs to reach an external system">
  <Fragment slot="without">
    It fumbles the authentication, then hits the obscure error that cost you an hour last year.

    *It costs the hour again.*
  </Fragment>
  <Fragment slot="with">
    It reads that system's document first — commands, credentials handling, and the failure modes you've already met.

    *The hour was spent once.*
  </Fragment>
</Compare>

---

## So what is it, exactly?

A **git repository of markdown files** that your agent loads as context at the start of every session. No database, no service, no daemon, no account.

Three ideas hold it together:

- **Knowledge** is durable — how your stack is set up, which conventions hold, what you decided and why. It's kept current.
- **Workspaces** are time-bound — an investigation, a migration, a piece of work with an end. They're allowed to go stale, which is what keeps the durable half trustworthy.
- **Scopes** decide what loads. A rule for everyone sits at the top; one that applies only to you, or only on one machine, sits further down. Your agent inherits the chain, and the innermost wins.

<CardGrid stagger>
  <Card title="Any agent" icon="puzzle">
    Universal context lives in `AGENTS.md`. Per-agent quirks live in `CLAUDE.md`, `CODEX.md`, or `OPENCLAW.md` sidecars. Switching agents doesn't mean rewriting anything.
  </Card>
  <Card title="Yours outright" icon="approve-check">
    You don't fork this repo and you never merge from it. An agent reads the concept and builds *your* exobrain. It has no upstream, so nothing here can break it.
  </Card>
  <Card title="Readable" icon="document">
    Every fact is a line of markdown in a git history. When your agent gets something wrong you can see which line taught it that, and fix it.
  </Card>
  <Card title="Scales down" icon="setting">
    A useful exobrain is one `AGENTS.md` and one knowledge file. The rest exists when you need it and stays out of the way when you don't.
  </Card>
</CardGrid>

## Read on

<LinkCard
  title="Why an exobrain"
  description="The failure modes above, explained properly — plus an honest comparison against a flat AGENTS.md, built-in agent memory, and a wiki. Including when not to bother."
  href="/why/"
/>

<LinkCard
  title="How it gets used"
  description="What actually accumulates in one, and what it feels like day to day."
  href="/how-its-used/"
/>

<LinkCard
  title="Get started"
  description="Paste one prompt into an agent in an empty folder. It interviews you and scaffolds your exobrain in place."
  href="/start/"
/>

<LinkCard
  title="Questions"
  description="Is this lock-in? What if the project stops? Do I have to keep it updated? Answered plainly."
  href="/questions/"
/>
