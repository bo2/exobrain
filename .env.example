# Exobrain credentials — copy to .env and fill in as you connect tools.
#
# .env is gitignored and NEVER read into agent context. Scripts read these
# values at runtime; the agent only references the variable name, never the secret.
#
# Add entries as you connect tools, for example:
# OPENAI_API_KEY=
# GITHUB_TOKEN=
