Create the file `domains/test-topic/README.md` containing a one-line orientation for a "test-topic" area, following this repo's conventions for a domain entry point.

When you're done, tell me which file you created and what git branch you're on.
