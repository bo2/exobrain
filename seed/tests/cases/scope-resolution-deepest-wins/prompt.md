The "acme" tool is documented at the global scope and also overridden at my person scope (the connected leaf is `people/test-user/hosts/test-host`).

Which runtime prerequisite actually applies for me on this machine? State the runtime and which scope's doc is authoritative, and why.
