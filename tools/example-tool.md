# Example Tool

One-line purpose — what this tool lets the agent read or do. This file is a non-functional template; copy it for a real tool, or delete it.

## At a glance
- **Prerequisites:** none
- **Platforms:** all
- **Credentials:** `EXAMPLE_TOKEN` — API token (`.env`)
- **Scripts:** none
- **Use cases:** general

## Setup
1. Obtain an API token from the service.
2. Add `EXAMPLE_TOKEN=<token>` to `.env` at the repo root (gitignored).

## Verify
Run the tool's read path and confirm it returns without an auth error. Replace this with a concrete command and its pass criteria.

## Troubleshooting
- **401 / auth error** — the token is missing or expired; re-add `EXAMPLE_TOKEN` to `.env`.
