---
name: exobrain-persist
description: "The default way completed exobrain work lands: worktree → timeline updates → one commit per logical change → push → PR → squash-merge → update the main copy → worktree cleanup, run as one command (scripts/persist.sh). Runs automatically at each completed logical change (standing authorization in AGENTS.md); also invokable by saying 'persist' / 'save' / 'land' / 'ship'."
---

# Persist

Land a completed logical change on the default branch in one shot, through a pull request. This is the **default** flow: `AGENTS.md` § Git workflow grants standing authorization to run it without being asked once a logical change is genuinely done — when more edits or a correction are likely (mid-task, or the user is still iterating), hold and confirm first. The user can also invoke it by name. Follow that git workflow throughout.

1. **Get onto a branch.** Normally you're already in the worktree you started the work in (`AGENTS.md` § Git workflow — worktree-first), so this is a no-op. Recovery only — if work wrongly began in-place on the default branch, move it to a branch first (`git stash -u` → `scripts/create-worktree.sh <branch>` → `cd` into it → `git stash pop`); worktree-first means this shouldn't happen.
2. **Commit** — one commit per logical change, imperative mood. Leaving the work uncommitted and passing `-m` to the script in step 4 is equivalent.
3. **Verify behavior-shaping changes.** Pure docs (`knowledge/`, `tools/`, `workspaces/`) are exempt; two other kinds of change are verified before they land.
   - **Agent specs, at any scope** — every `AGENTS.md`, `CLAUDE.md`/`CODEX.md` sidecar, and `SKILL.md`. From the worktree, run the behavioral suite (the `exobrain-tests` skill) with `--working-tree` over the cases this change could move, adding `--scope <leaf>` for a spec below the global scope. When no case exercises the rule you changed, add one: under the spec's own scope in `<scope>/tests/behavior/` for a scope rule, in the suite's `cases/` for a global one. Fix any case below threshold, then re-run. The land reads the record each run leaves: it refuses a spec that no passing run tested as it now stands — with the spec's scope wired and, for a sidecar, that sidecar's agent — and lists the qualifying runs in the PR. A session that can't run the suite (a chat turn, a scheduled job) leaves its spec change waiting on its branch for an attended session to verify and land; its reply says the change is pending, not live.
   - **The rest of the shared machinery** — root `scripts/`, the code in a root `skills/` skill, the registries (`skills.json` / `scopes.json` / `skills.schema.json`), and `OPENCLAW.md` sidecars at any scope. The script runs the deterministic unit suite (`skills/exobrain-tests/unit/run.sh`) itself; the agent-judged half is yours — the behavior suite where the change alters what agents do. The script refuses to land such a diff until `--machinery-verified` asserts that this happened; its refusal names the flag.

   For a global-scope change *meant to alter behavior* (a reworded rule, a new or edited skill), also run `exobrain-ab` to confirm it actually moves behavior — a spec edit that changes nothing isn't worth landing. You judge which cases are relevant and whether a change is behavior-altering.
4. **Land** — from inside the worktree. Which form depends on who is waiting:

   ```bash
   scripts/persist.sh --detach -m "<commit message>"     # a chat turn: someone is waiting for a reply
   scripts/persist.sh [-m "<commit message>"] [--timeline "<one-line summary>"]   # a terminal or scheduled session
   ```

   Both forms take `--machinery-verified` when step 3 applies. In a chat turn (the reply goes to a chat channel) always use `--detach`: it commits, returns at once, and finishes the land in a process of its own, so the reply goes out on the commit and names no PR, link, or merge — none exists yet. Never run the plain form there, and never wait for or poll the detached land. In a terminal or scheduled session run the plain form and report the PR it prints.

   **Hand over what the diff doesn't show.** When this session knows something the change's files don't say — what the person asked for in their own words, which file you decided owns a fact, what you were unsure about, what you deliberately left out — write it to `tmp/<anything>.md` in the worktree and pass `--context <that path>`. It becomes a "Source context" section in the PR body, which is what survives: the worktree is removed as the land finishes. Whoever later acts on the review's findings reads it (`exobrain-repair-findings`). Skip it when the diff speaks for itself.

   The script does the rest in order, each step skipped when already done, so re-running after an interruption resumes rather than repeats: appends a row (`| date | author | summary |`) to the `TIMELINE.md` of every workspace or knowledge domain touched whose `README.md` frontmatter carries `timeline: true` (creating the file if absent; `--timeline` sets the summary, else the commit subject — see [`knowledge/exobrain/domains.md`](../../knowledge/exobrain/domains.md) → "Timeline tracking") → commits anything still uncommitted → `scripts/validate-exobrain.sh` → `scripts/authoring-review.sh` (the LLM judgment layer over changed spec/domain files; skippable with `EXOBRAIN_SKIP_AUTHORING_REVIEW=1`) → pushes the branch → opens the PR (title from the branch's first commit unless `--title` says otherwise; body from the commits) → squash-merges it (a merge refused for a conflict merges the default branch into the branch and retries once) → fast-forwards the main checkout → removes the worktree and deletes the branch locally and on the remote. Without a remote, push through merge collapse into a fast-forward of the local default branch.

   A failed step stops the script with the branch keeping every step already made: fix what it named (a validator violation, an authoring finding, a conflict left in the worktree), amend the still-unpushed commit if the commit is what changed, and re-run. A detached or swept land has nobody to fix a finding, so there the authoring review's findings are posted as a comment review on the PR instead of blocking; its proof gate and the validator block either way, and the `exobrain-repair-findings` skill is what later clears them. `--dry-run` prints the plan.
5. **Continuing the same work?** Create a fresh worktree off the updated default branch before further edits. Ask if the intent isn't clear.

**Landing later.** The script claims the worktree first, so a run that dies leaves a claim the sweep picks up (a claim made with `--machinery-verified` carries the assertion). `scripts/persist.sh --sweep` (from any checkout of the repo) lands every claimed worktree and every clean worktree that is ahead of the default branch and untouched for an hour. It leaves alone a dirty worktree and one whose machinery diff carries no step-3 assertion, and reports either as stale once it has waited two days (`PERSIST_STALE_DAYS`) — which fails the sweep, so a scheduled job's alert names it. A host that runs chat sessions schedules the sweep in its `crons.json`.
